const basePath = '/';

const reactTutorial = `${basePath}reactTutorial`;
const dragAndDrop = `${basePath}dragAndDrop`;

export const Routes = {
  basePath,
  paths: {
    reactTutorial,
    dragAndDrop,
  },
} as const;
